@model Proiect.Models.Chef

@{
    ViewData["Title"] = "Chef Details";
}

<h1>Chef Details</h1>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ChefName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ChefName)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-primary" asp-action="Edit" asp-route-id="@Model?.ID">Edit Chef</a> |
    <a asp-action="Index">Back to Chef List</a>
</div>
